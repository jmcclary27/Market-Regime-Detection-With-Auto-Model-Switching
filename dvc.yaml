stages:
  offline_pipeline:
    cmd: python -m src.pipeline.run --offline --run-ts ${run_ts}
    deps:
      - src/pipeline
      - src/config/settings.yaml
      - src/demo/run.py
      - params.yaml
    params:
      - run_ts
    always_changed: true
